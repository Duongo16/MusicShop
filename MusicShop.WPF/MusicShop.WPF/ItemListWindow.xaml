<Window x:Class="MusicShop.WPF.ItemListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Items — Manager" Height="600" Width="900" WindowStartupLocation="CenterScreen">
    <DockPanel LastChildFill="True" Margin="12">
        <!-- Toolbar -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBox Width="260" Margin="0,0,8,0" VerticalAlignment="Center"
                     Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" 
                    />
            <Button Content="Search" Margin="0,0,8,0" Padding="16,4" Command="{Binding SearchCommand}" />
            <Button Content="Refresh" Padding="16,4" Command="{Binding RefreshCommand}" />
            <TextBlock Text="{Binding PageInfo}" Margin="16,0,0,0" VerticalAlignment="Center" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="◀ Prev" Margin="16,0,8,0" Padding="12,4" Command="{Binding PrevPageCommand}" />
                <Button Content="Next ▶" Padding="12,4" Command="{Binding NextPageCommand}" />
            </StackPanel>
        </StackPanel>

        <!-- Grid -->
        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="220" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="140" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
